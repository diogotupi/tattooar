/**
 * Ponto único de entrada para o compilador Mind AR.
 * Importar a partir de `bank.ts` com caminho relativo faz o Vite resolver e
 * servir o módulo corretamente em dev (incl. `npm run dev -- --host`).
 */
export { Compiler } from "mind-ar/src/image-target/compiler.js";
