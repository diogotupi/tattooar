# Servidor local (npm run dev)
VITE_BASE_PATH=/

# JSON servido a partir de `public/` (opcional). Omissão = defeito em main.ts (bundles/born-to-be.json).
# VITE_PUBLIC_BUNDLE=bundles/onca-clipping.json
# VITE_PUBLIC_BUNDLE=bundles/outro.json

# Opcional: mesma chave que em produção para testar o ecrã de admin localmente.
# VITE_ADMIN_KEY=
