# mind-ar depende de `canvas` (Node nativo). No Windows + Node recente não há
# binário pré-compilado e exige Python/Build Tools. O nosso app só usa MindAR no
# browser — scripts de instalação nativos não são necessários.
ignore-scripts=true
